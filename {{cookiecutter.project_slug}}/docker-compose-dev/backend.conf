{
    {{ cookiecutter.project_slug }}: {
        elasticsearch: {
            hosts: [ "elastic-dev" ],
            use_ssl: True,
            ca_certs: "/euler/root-ca.pem",
            ssl_show_warn: False
        }
    }
}